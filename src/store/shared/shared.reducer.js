const initialState = Object.freeze({
    sessionId: '',
    stage: 0,
    admin: null,
    team1: [],
    team2: [],
    team3: []
});

export default (state = initialState, action) => {
    return state;
};
